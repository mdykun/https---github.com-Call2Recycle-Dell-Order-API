namespace RecycleAPI.Models
{
    public class ValidStateLookup
    {
        private static string[] validStates = {""};
        public static bool IsValid(string stateValue)
        {
            // check the list of states and determine if it is valid
            return true;
        }
    }
}
